{
  extends: [
    "config:best-practices"
  ],
  dependencyDashboard: false,
  // Files distributed by Terraform must remain unchanged in each repository.
  // This avoids conflicts with the source of truth, including digest pinning.
  ignorePaths: [
    ".github/workflows/cruise.yml",
    ".woodpecker/cruise.yaml",
    ".woodpecker/cruise-run.sh",
    "cruise.yaml",
  ],
  minimumReleaseAge: "3 days",
  lockFileMaintenance: {
    minimumReleaseAge: null,
  },
  vulnerabilityAlerts: {
    minimumReleaseAge: null,
  },
  packageRules: [
    {
      matchUpdateTypes: [
        "minor",
        "patch",
        "digest",
        "pinDigest"
      ],
      groupName: "all minor, patch, and digest updates",
      groupSlug: "all-minor-patch-digest-updates",
    },
    {
      matchManagers: [
        "github-actions"
      ],
      minimumReleaseAge: "7 days"
    }
  ]
}
